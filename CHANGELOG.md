# Change Log for Gigs4You

## Unreleased
- Introduced a Product-grade UI approach: Card-based KPI tiles with hover AI hints on the dashboard.
- Added OTP delivery improvements (SMS) for mobile login and channel signaling for OTP path.
- Implemented a robust org access path for non-super-admin users.
- Added a small admin action: reverse wallet transaction (server-side) with an API endpoint (UI not wired yet).
- Introduced a lightweight PM framework for in-repo change tracking (docs skeleton to come).

## [Next versions]
- Plan user-visible features and ensure you have a clean, backward-compatible rollout.
